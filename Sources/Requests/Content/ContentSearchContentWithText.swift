//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

extension BungieNetAPI.Content {

    /** Gets content based on querystring information passed in. Provides basic search and text search capabilities. */
    public enum ContentSearchContentWithText {

        public static let service = APIService<Response>(id: "Content.SearchContentWithText", tag: "Content", method: "GET", path: "/Content/Search/{locale}/", hasBody: false)

        public final class Request: APIRequest<Response> {

            public struct Options {

                /** Content type tag: Help, News, etc. Supply multiple ctypes separated by space. */
                public var ctype: String?

                /** Page number for the search results, starting with page 1. */
                public var currentpage: Int?

                /** Not used. */
                public var head: Bool?

                public var locale: String

                /** Word or phrase for the search. */
                public var searchtext: String?

                /** For analytics, hint at the part of the app that triggered the search. Optional. */
                public var source: String?

                /** Tag used on the content to be searched. */
                public var tag: String?

                public init(ctype: String? = nil, currentpage: Int? = nil, head: Bool? = nil, locale: String, searchtext: String? = nil, source: String? = nil, tag: String? = nil) {
                    self.ctype = ctype
                    self.currentpage = currentpage
                    self.head = head
                    self.locale = locale
                    self.searchtext = searchtext
                    self.source = source
                    self.tag = tag
                }
            }

            public var options: Options

            public init(options: Options) {
                self.options = options
                super.init(service: ContentSearchContentWithText.service)
            }

            /// convenience initialiser so an Option doesn't have to be created
            public convenience init(ctype: String? = nil, currentpage: Int? = nil, head: Bool? = nil, locale: String, searchtext: String? = nil, source: String? = nil, tag: String? = nil) {
                let options = Options(ctype: ctype, currentpage: currentpage, head: head, locale: locale, searchtext: searchtext, source: source, tag: tag)
                self.init(options: options)
            }

            public override var path: String {
                return super.path.replacingOccurrences(of: "{" + "locale" + "}", with: "\(self.options.locale)")
            }

            public override var queryParameters: [String: Any] {
                var params: [String: Any] = [:]
                if let ctype = options.ctype {
                  params["ctype"] = ctype
                }
                if let currentpage = options.currentpage {
                  params["currentpage"] = currentpage
                }
                if let head = options.head {
                  params["head"] = head
                }
                if let searchtext = options.searchtext {
                  params["searchtext"] = searchtext
                }
                if let source = options.source {
                  params["source"] = source
                }
                if let tag = options.tag {
                  params["tag"] = tag
                }
                return params
            }
        }

        public enum Response: APIResponseValue, CustomStringConvertible, CustomDebugStringConvertible {

            /** Gets content based on querystring information passed in. Provides basic search and text search capabilities. */
            public struct Status200: APIModel {

                public var detailedErrorTrace: String?

                public var errorCode: Int?

                public var errorStatus: String?

                public var message: String?

                public var messageData: [String: String]?

                public var response: SearchResultOfContentItemPublicContract?

                public var throttleSeconds: Int?

                public init(detailedErrorTrace: String? = nil, errorCode: Int? = nil, errorStatus: String? = nil, message: String? = nil, messageData: [String: String]? = nil, response: SearchResultOfContentItemPublicContract? = nil, throttleSeconds: Int? = nil) {
                    self.detailedErrorTrace = detailedErrorTrace
                    self.errorCode = errorCode
                    self.errorStatus = errorStatus
                    self.message = message
                    self.messageData = messageData
                    self.response = response
                    self.throttleSeconds = throttleSeconds
                }

                public init(from decoder: Decoder) throws {
                    let container = try decoder.container(keyedBy: StringCodingKey.self)

                    detailedErrorTrace = try container.decodeIfPresent("DetailedErrorTrace")
                    errorCode = try container.decodeIfPresent("ErrorCode")
                    errorStatus = try container.decodeIfPresent("ErrorStatus")
                    message = try container.decodeIfPresent("Message")
                    messageData = try container.decodeIfPresent("MessageData")
                    response = try container.decodeIfPresent("Response")
                    throttleSeconds = try container.decodeIfPresent("ThrottleSeconds")
                }

                public func encode(to encoder: Encoder) throws {
                    var container = encoder.container(keyedBy: StringCodingKey.self)

                    try container.encodeIfPresent(detailedErrorTrace, forKey: "DetailedErrorTrace")
                    try container.encodeIfPresent(errorCode, forKey: "ErrorCode")
                    try container.encodeIfPresent(errorStatus, forKey: "ErrorStatus")
                    try container.encodeIfPresent(message, forKey: "Message")
                    try container.encodeIfPresent(messageData, forKey: "MessageData")
                    try container.encodeIfPresent(response, forKey: "Response")
                    try container.encodeIfPresent(throttleSeconds, forKey: "ThrottleSeconds")
                }

                public static func == (lhs: Status200, rhs: Status200) -> Bool {
                    guard lhs.detailedErrorTrace == rhs.detailedErrorTrace else { return false }
                    guard lhs.errorCode == rhs.errorCode else { return false }
                    guard lhs.errorStatus == rhs.errorStatus else { return false }
                    guard lhs.message == rhs.message else { return false }
                    guard lhs.messageData == rhs.messageData else { return false }
                    guard lhs.response == rhs.response else { return false }
                    guard lhs.throttleSeconds == rhs.throttleSeconds else { return false }
                    return true
                }
            }
            public typealias SuccessType = Status200

            /** Look at the Response property for more information about the nature of this response */
            case status200(Status200)

            public var success: Status200? {
                switch self {
                case .status200(let response): return response
                }
            }

            public var response: Any {
                switch self {
                case .status200(let response): return response
                }
            }

            public var statusCode: Int {
                switch self {
                case .status200: return 200
                }
            }

            public var successful: Bool {
                switch self {
                case .status200: return true
                }
            }

            public init(statusCode: Int, data: Data, decoder: ResponseDecoder) throws {
                switch statusCode {
                case 200: self = try .status200(decoder.decode(Status200.self, from: data))
                default: throw APIClientError.unexpectedStatusCode(statusCode: statusCode, data: data)
                }
            }

            public var description: String {
                return "\(statusCode) \(successful ? "success" : "failure")"
            }

            public var debugDescription: String {
                var string = description
                let responseString = "\(response)"
                if responseString != "()" {
                    string += "\n\(responseString)"
                }
                return string
            }
        }
    }
}
