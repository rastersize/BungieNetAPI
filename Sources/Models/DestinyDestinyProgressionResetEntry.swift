//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Represents a season and the number of resets you had in that season.
 We do not necessarily - even for progressions with resets - track it over all seasons. So be careful and check the season numbers being returned. */
public struct DestinyDestinyProgressionResetEntry: APIModel {

    public var resets: Int?

    public var season: Int?

    public init(resets: Int? = nil, season: Int? = nil) {
        self.resets = resets
        self.season = season
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        resets = try container.decodeIfPresent("resets")
        season = try container.decodeIfPresent("season")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(resets, forKey: "resets")
        try container.encodeIfPresent(season, forKey: "season")
    }

    public static func == (lhs: DestinyDestinyProgressionResetEntry, rhs: DestinyDestinyProgressionResetEntry) -> Bool {
        guard lhs.resets == rhs.resets else { return false }
        guard lhs.season == rhs.season else { return false }
        return true
    }
}
