//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** This appears to be information used when rendering rewards. We don't currently use it on BNet. */
public struct DestinyDefinitionsDestinyItemSummaryBlockDefinition: APIModel {

    /** Apparently when rendering an item in a reward, this should be used as a sort priority. We're not doing it presently. */
    public var sortPriority: Int?

    public init(sortPriority: Int? = nil) {
        self.sortPriority = sortPriority
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        sortPriority = try container.decodeIfPresent("sortPriority")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(sortPriority, forKey: "sortPriority")
    }

    public static func == (lhs: DestinyDefinitionsDestinyItemSummaryBlockDefinition, rhs: DestinyDefinitionsDestinyItemSummaryBlockDefinition) -> Bool {
        guard lhs.sortPriority == rhs.sortPriority else { return false }
        return true
    }
}
