//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** This definition represents an "Activity Mode" as it exists in the Historical Stats endpoints. An individual Activity Mode represents a collection of activities that are played in a certain way. For example, Nightfall Strikes are part of a "Nightfall" activity mode, and any activities played as the PVP mode "Clash" are part of the "Clash activity mode.
Activity modes are nested under each other in a hierarchy, so that if you ask for - for example - "AllPvP", you will get any PVP activities that the user has played, regardless of what specific PVP mode was being played. */
public struct DestinyDefinitionsDestinyActivityModeDefinition: APIModel {

    /** The type of play being performed in broad terms (PVP, PVE) */
    public var activityModeCategory: Int?

    /** If this exists, the mode has specific Activities (referred to by the Key) that should instead map to other Activity Modes when they are played. This was useful in D1 for Private Matches, where we wanted to have Private Matches as an activity mode while still referring to the specific mode being played. */
    public var activityModeMappings: [String: Int]?

    /** If FALSE, we want to ignore this type when we're showing activity modes in BNet UI. It will still be returned in case 3rd parties want to use it for any purpose. */
    public var display: Bool?

    public var displayProperties: DestinyDefinitionsCommonDestinyDisplayPropertiesDefinition?

    /** A Friendly identifier you can use for referring to this Activity Mode. We really only used this in our URLs, so... you know, take that for whatever it's worth. */
    public var friendlyName: String?

    /** The unique identifier for this entity. Guaranteed to be unique for the type of entity, but not globally.
When entities refer to each other in Destiny content, it is this hash that they are referring to. */
    public var hash: Int?

    /** The index of the entity as it was found in the investment tables. */
    public var index: Int?

    /** If true, this mode is an aggregation of other, more specific modes rather than being a mode in itself. This includes modes that group Features/Events rather than Gameplay, such as Trials of The Nine: Trials of the Nine being an Event that is interesting to see aggregate data for, but when you play the activities within Trials of the Nine they are more specific activity modes such as Clash. */
    public var isAggregateMode: Bool?

    /** If True, this mode has oppositional teams fighting against each other rather than "Free-For-All" or Co-operative modes of play.
Note that Aggregate modes are never marked as team based, even if they happen to be team based at the moment. At any time, an aggregate whose subordinates are only team based could be changed so that one or more aren't team based, and then this boolean won't make much sense (the aggregation would become "sometimes team based"). Let's not deal with that right now. */
    public var isTeamBased: Bool?

    /** The Enumeration value for this Activity Mode. Pass this identifier into Stats endpoints to get aggregate stats for this mode. */
    public var modeType: Int?

    /** The relative ordering of activity modes. */
    public var order: Int?

    /** The hash identifiers of the DestinyActivityModeDefinitions that represent all of the "parent" modes for this mode. For instance, the Nightfall Mode is also a member of AllStrikes and AllPvE. */
    public var parentHashes: [Int]?

    /** If this activity mode has a related PGCR image, this will be the path to said image. */
    public var pgcrImage: String?

    /** If this is true, then there is an entity with this identifier/type combination, but BNet is not yet allowed to show it. Sorry! */
    public var redacted: Bool?

    public init(activityModeCategory: Int? = nil, activityModeMappings: [String: Int]? = nil, display: Bool? = nil, displayProperties: DestinyDefinitionsCommonDestinyDisplayPropertiesDefinition? = nil, friendlyName: String? = nil, hash: Int? = nil, index: Int? = nil, isAggregateMode: Bool? = nil, isTeamBased: Bool? = nil, modeType: Int? = nil, order: Int? = nil, parentHashes: [Int]? = nil, pgcrImage: String? = nil, redacted: Bool? = nil) {
        self.activityModeCategory = activityModeCategory
        self.activityModeMappings = activityModeMappings
        self.display = display
        self.displayProperties = displayProperties
        self.friendlyName = friendlyName
        self.hash = hash
        self.index = index
        self.isAggregateMode = isAggregateMode
        self.isTeamBased = isTeamBased
        self.modeType = modeType
        self.order = order
        self.parentHashes = parentHashes
        self.pgcrImage = pgcrImage
        self.redacted = redacted
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        activityModeCategory = try container.decodeIfPresent("activityModeCategory")
        activityModeMappings = try container.decodeIfPresent("activityModeMappings")
        display = try container.decodeIfPresent("display")
        displayProperties = try container.decodeIfPresent("displayProperties")
        friendlyName = try container.decodeIfPresent("friendlyName")
        hash = try container.decodeIfPresent("hash")
        index = try container.decodeIfPresent("index")
        isAggregateMode = try container.decodeIfPresent("isAggregateMode")
        isTeamBased = try container.decodeIfPresent("isTeamBased")
        modeType = try container.decodeIfPresent("modeType")
        order = try container.decodeIfPresent("order")
        parentHashes = try container.decodeArrayIfPresent("parentHashes")
        pgcrImage = try container.decodeIfPresent("pgcrImage")
        redacted = try container.decodeIfPresent("redacted")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(activityModeCategory, forKey: "activityModeCategory")
        try container.encodeIfPresent(activityModeMappings, forKey: "activityModeMappings")
        try container.encodeIfPresent(display, forKey: "display")
        try container.encodeIfPresent(displayProperties, forKey: "displayProperties")
        try container.encodeIfPresent(friendlyName, forKey: "friendlyName")
        try container.encodeIfPresent(hash, forKey: "hash")
        try container.encodeIfPresent(index, forKey: "index")
        try container.encodeIfPresent(isAggregateMode, forKey: "isAggregateMode")
        try container.encodeIfPresent(isTeamBased, forKey: "isTeamBased")
        try container.encodeIfPresent(modeType, forKey: "modeType")
        try container.encodeIfPresent(order, forKey: "order")
        try container.encodeIfPresent(parentHashes, forKey: "parentHashes")
        try container.encodeIfPresent(pgcrImage, forKey: "pgcrImage")
        try container.encodeIfPresent(redacted, forKey: "redacted")
    }

    public static func == (lhs: DestinyDefinitionsDestinyActivityModeDefinition, rhs: DestinyDefinitionsDestinyActivityModeDefinition) -> Bool {
        guard lhs.activityModeCategory == rhs.activityModeCategory else { return false }
        guard lhs.activityModeMappings == rhs.activityModeMappings else { return false }
        guard lhs.display == rhs.display else { return false }
        guard lhs.displayProperties == rhs.displayProperties else { return false }
        guard lhs.friendlyName == rhs.friendlyName else { return false }
        guard lhs.hash == rhs.hash else { return false }
        guard lhs.index == rhs.index else { return false }
        guard lhs.isAggregateMode == rhs.isAggregateMode else { return false }
        guard lhs.isTeamBased == rhs.isTeamBased else { return false }
        guard lhs.modeType == rhs.modeType else { return false }
        guard lhs.order == rhs.order else { return false }
        guard lhs.parentHashes == rhs.parentHashes else { return false }
        guard lhs.pgcrImage == rhs.pgcrImage else { return false }
        guard lhs.redacted == rhs.redacted else { return false }
        return true
    }
}
