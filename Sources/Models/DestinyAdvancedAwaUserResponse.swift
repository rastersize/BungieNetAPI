//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public struct DestinyAdvancedAwaUserResponse: APIModel {

    /** Correlation ID of the request */
    public var correlationId: String?

    /** Secret nonce received via the PUSH notification. */
    public var nonce: [File]?

    /** Indication of the selection the user has made (Approving or rejecting the action) */
    public var selection: Int?

    public init(correlationId: String? = nil, nonce: [File]? = nil, selection: Int? = nil) {
        self.correlationId = correlationId
        self.nonce = nonce
        self.selection = selection
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        correlationId = try container.decodeIfPresent("correlationId")
        nonce = try container.decodeArrayIfPresent("nonce")
        selection = try container.decodeIfPresent("selection")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(correlationId, forKey: "correlationId")
        try container.encodeIfPresent(nonce, forKey: "nonce")
        try container.encodeIfPresent(selection, forKey: "selection")
    }

    public static func == (lhs: DestinyAdvancedAwaUserResponse, rhs: DestinyAdvancedAwaUserResponse) -> Bool {
        guard lhs.correlationId == rhs.correlationId else { return false }
        guard lhs.nonce == rhs.nonce else { return false }
        guard lhs.selection == rhs.selection else { return false }
        return true
    }
}
