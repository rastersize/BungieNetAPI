//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Represents whatever information we can return about an explicit phase in an activity. In the future, I hope we'll have more than just "guh, you done gone and did something," but for the forseeable future that's all we've got. I'm making it more than just a list of booleans out of that overly-optimistic hope. */
public struct DestinyMilestonesDestinyMilestoneActivityPhase: APIModel {

    /** Indicates if the phase has been completed. */
    public var complete: Bool?

    /** In DestinyActivityDefinition, if the activity has phases, there will be a set of phases defined in the "insertionPoints" property. This is the hash that maps to that phase. */
    public var phaseHash: Int?

    public init(complete: Bool? = nil, phaseHash: Int? = nil) {
        self.complete = complete
        self.phaseHash = phaseHash
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        complete = try container.decodeIfPresent("complete")
        phaseHash = try container.decodeIfPresent("phaseHash")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(complete, forKey: "complete")
        try container.encodeIfPresent(phaseHash, forKey: "phaseHash")
    }

    public static func == (lhs: DestinyMilestonesDestinyMilestoneActivityPhase, rhs: DestinyMilestonesDestinyMilestoneActivityPhase) -> Bool {
        guard lhs.complete == rhs.complete else { return false }
        guard lhs.phaseHash == rhs.phaseHash else { return false }
        return true
    }
}
