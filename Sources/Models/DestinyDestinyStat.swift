//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Represents a stat on an item *or* Character (NOT a Historical Stat, but a physical attribute stat like Attack, Defense etc...) */
public struct DestinyDestinyStat: APIModel {

    /** The hash identifier for the Stat. Use it to look up the DestinyStatDefinition for static data about the stat. */
    public var statHash: Int?

    /** The current value of the Stat. */
    public var value: Int?

    public init(statHash: Int? = nil, value: Int? = nil) {
        self.statHash = statHash
        self.value = value
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        statHash = try container.decodeIfPresent("statHash")
        value = try container.decodeIfPresent("value")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(statHash, forKey: "statHash")
        try container.encodeIfPresent(value, forKey: "value")
    }

    public static func == (lhs: DestinyDestinyStat, rhs: DestinyDestinyStat) -> Bool {
        guard lhs.statHash == rhs.statHash else { return false }
        guard lhs.value == rhs.value else { return false }
        return true
    }
}
