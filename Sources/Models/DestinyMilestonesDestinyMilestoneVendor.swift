//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** If a Milestone has one or more Vendors that are relevant to it, this will contain information about that vendor that you can choose to show. */
public struct DestinyMilestonesDestinyMilestoneVendor: APIModel {

    /** If this vendor is featuring a specific item for this event, this will be the hash identifier of that item. I'm taking bets now on how long we go before this needs to be a list or some other, more complex representation instead and I deprecate this too. I'm going to go with 5 months. Calling it now, 2017-09-14 at 9:46pm PST. */
    public var previewItemHash: Int?

    /** The hash identifier of the Vendor related to this Milestone. You can show useful things from this, such as thier Faction icon or whatever you might care about. */
    public var vendorHash: Int?

    public init(previewItemHash: Int? = nil, vendorHash: Int? = nil) {
        self.previewItemHash = previewItemHash
        self.vendorHash = vendorHash
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        previewItemHash = try container.decodeIfPresent("previewItemHash")
        vendorHash = try container.decodeIfPresent("vendorHash")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(previewItemHash, forKey: "previewItemHash")
        try container.encodeIfPresent(vendorHash, forKey: "vendorHash")
    }

    public static func == (lhs: DestinyMilestonesDestinyMilestoneVendor, rhs: DestinyMilestonesDestinyMilestoneVendor) -> Bool {
        guard lhs.previewItemHash == rhs.previewItemHash else { return false }
        guard lhs.vendorHash == rhs.vendorHash else { return false }
        return true
    }
}
