//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** These definitions represent faction vendors at different points in the game.
A single faction may contain multiple vendors, or the same vendor available at two different locations. */
public struct DestinyDefinitionsDestinyFactionVendorDefinition: APIModel {

    /** The relative path to the background image representing this Vendor at this location, for use in a banner. */
    public var backgroundImagePath: String?

    /** The hash identifier for a Destination at which this vendor may be located. Each destination where a Vendor may exist will only ever have a single entry. */
    public var destinationHash: Int?

    /** The faction vendor hash. */
    public var vendorHash: Int?

    public init(backgroundImagePath: String? = nil, destinationHash: Int? = nil, vendorHash: Int? = nil) {
        self.backgroundImagePath = backgroundImagePath
        self.destinationHash = destinationHash
        self.vendorHash = vendorHash
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        backgroundImagePath = try container.decodeIfPresent("backgroundImagePath")
        destinationHash = try container.decodeIfPresent("destinationHash")
        vendorHash = try container.decodeIfPresent("vendorHash")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(backgroundImagePath, forKey: "backgroundImagePath")
        try container.encodeIfPresent(destinationHash, forKey: "destinationHash")
        try container.encodeIfPresent(vendorHash, forKey: "vendorHash")
    }

    public static func == (lhs: DestinyDefinitionsDestinyFactionVendorDefinition, rhs: DestinyDefinitionsDestinyFactionVendorDefinition) -> Bool {
        guard lhs.backgroundImagePath == rhs.backgroundImagePath else { return false }
        guard lhs.destinationHash == rhs.destinationHash else { return false }
        guard lhs.vendorHash == rhs.vendorHash else { return false }
        return true
    }
}
