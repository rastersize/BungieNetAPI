//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Returns the detailed information about a Collectible Presentation Node and any Collectibles that are direct descendants. */
public struct DestinyResponsesDestinyCollectibleNodeDetailResponse: APIModel {

    /** Item components, keyed by the item hash of the items pointed at collectibles found under the requested Presentation Node.
NOTE: I had a lot of hemming and hawing about whether these should be keyed by collectible hash or item hash... but ultimately having it be keyed by item hash meant that UI that already uses DestinyItemComponentSet data wouldn't have to have a special override to do the collectible -> item lookup once you delve into an item's details, and it also meant that you didn't have to remember that the Hash being used as the key for plugSets was different from the Hash being used for the other Dictionaries. As a result, using the Item Hash felt like the least crappy solution.
We may all come to regret this decision. We will see.
COMPONENT TYPE: [See inside the DestinyItemComponentSet contract for component types.] */
    public var collectibleItemComponents: [String: Any]?

    /** COMPONENT TYPE: Collectibles */
    public var collectibles: [String: Any]?

    public init(collectibleItemComponents: [String: Any]? = nil, collectibles: [String: Any]? = nil) {
        self.collectibleItemComponents = collectibleItemComponents
        self.collectibles = collectibles
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        collectibleItemComponents = try container.decodeAnyIfPresent("collectibleItemComponents")
        collectibles = try container.decodeAnyIfPresent("collectibles")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeAnyIfPresent(collectibleItemComponents, forKey: "collectibleItemComponents")
        try container.encodeAnyIfPresent(collectibles, forKey: "collectibles")
    }

    public static func == (lhs: DestinyResponsesDestinyCollectibleNodeDetailResponse, rhs: DestinyResponsesDestinyCollectibleNodeDetailResponse) -> Bool {
        guard NSDictionary(dictionary: lhs.collectibleItemComponents ?? [:]).isEqual(to: rhs.collectibleItemComponents ?? [:]) else { return false }
        guard NSDictionary(dictionary: lhs.collectibles ?? [:]).isEqual(to: rhs.collectibles ?? [:]) else { return false }
        return true
    }
}
