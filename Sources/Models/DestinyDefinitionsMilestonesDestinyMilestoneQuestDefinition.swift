//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Any data we need to figure out whether this Quest Item is the currently active one for the conceptual Milestone. Even just typing this description, I already regret it. */
public struct DestinyDefinitionsMilestonesDestinyMilestoneQuestDefinition: APIModel {

    /** The full set of all possible "conceptual activities" that are related to this Milestone. Tiers or alternative modes of play within these conceptual activities will be defined as sub-entities. Keyed by the Conceptual Activity Hash. Use the key to look up DestinyActivityDefinition. */
    public var activities: [String: DestinyDefinitionsMilestonesDestinyMilestoneActivityDefinition]?

    /** Sometimes, a Milestone's quest is related to an entire Destination rather than a specific activity. In that situation, this will be the hash of that Destination. Hotspots are currently the only Milestones that expose this data, but that does not preclude this data from being returned for other Milestones in the future. */
    public var destinationHash: Int?

    /** The individual quests may have different definitions from the overall milestone: if there's a specific active quest, use these displayProperties instead of that of the overall DestinyMilestoneDefinition. */
    public var displayProperties: [String: Any]?

    /** If populated, this image can be shown instead of the generic milestone's image when this quest is live, or it can be used to show a background image for the quest itself that differs from that of the Activity or the Milestone. */
    public var overrideImage: String?

    /** The item representing this Milestone quest. Use this hash to look up the DestinyInventoryItemDefinition for the quest to find its steps and human readable data. */
    public var questItemHash: Int?

    /** The rewards you will get for completing this quest, as best as we could extract them from our data. Sometimes, it'll be a decent amount of data. Sometimes, it's going to be sucky. Sorry. */
    public var questRewards: [String: Any]?

    public init(activities: [String: DestinyDefinitionsMilestonesDestinyMilestoneActivityDefinition]? = nil, destinationHash: Int? = nil, displayProperties: [String: Any]? = nil, overrideImage: String? = nil, questItemHash: Int? = nil, questRewards: [String: Any]? = nil) {
        self.activities = activities
        self.destinationHash = destinationHash
        self.displayProperties = displayProperties
        self.overrideImage = overrideImage
        self.questItemHash = questItemHash
        self.questRewards = questRewards
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        activities = try container.decodeIfPresent("activities")
        destinationHash = try container.decodeIfPresent("destinationHash")
        displayProperties = try container.decodeAnyIfPresent("displayProperties")
        overrideImage = try container.decodeIfPresent("overrideImage")
        questItemHash = try container.decodeIfPresent("questItemHash")
        questRewards = try container.decodeAnyIfPresent("questRewards")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(activities, forKey: "activities")
        try container.encodeIfPresent(destinationHash, forKey: "destinationHash")
        try container.encodeAnyIfPresent(displayProperties, forKey: "displayProperties")
        try container.encodeIfPresent(overrideImage, forKey: "overrideImage")
        try container.encodeIfPresent(questItemHash, forKey: "questItemHash")
        try container.encodeAnyIfPresent(questRewards, forKey: "questRewards")
    }

    public static func == (lhs: DestinyDefinitionsMilestonesDestinyMilestoneQuestDefinition, rhs: DestinyDefinitionsMilestonesDestinyMilestoneQuestDefinition) -> Bool {
        guard lhs.activities == rhs.activities else { return false }
        guard lhs.destinationHash == rhs.destinationHash else { return false }
        guard NSDictionary(dictionary: lhs.displayProperties ?? [:]).isEqual(to: rhs.displayProperties ?? [:]) else { return false }
        guard lhs.overrideImage == rhs.overrideImage else { return false }
        guard lhs.questItemHash == rhs.questItemHash else { return false }
        guard NSDictionary(dictionary: lhs.questRewards ?? [:]).isEqual(to: rhs.questRewards ?? [:]) else { return false }
        return true
    }
}
