//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

public struct ContentCommentSummary: APIModel {

    public var commentCount: Int?

    public var topicId: Int?

    public init(commentCount: Int? = nil, topicId: Int? = nil) {
        self.commentCount = commentCount
        self.topicId = topicId
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        commentCount = try container.decodeIfPresent("commentCount")
        topicId = try container.decodeIfPresent("topicId")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(commentCount, forKey: "commentCount")
        try container.encodeIfPresent(topicId, forKey: "topicId")
    }

    public static func == (lhs: ContentCommentSummary, rhs: ContentCommentSummary) -> Bool {
        guard lhs.commentCount == rhs.commentCount else { return false }
        guard lhs.topicId == rhs.topicId else { return false }
        return true
    }
}
