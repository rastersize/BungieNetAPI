//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Represents a single state that a graph node might end up in. Depending on what's going on in the game, graph nodes could be shown in different ways or even excluded from view entirely. */
public struct DestinyDefinitionsDirectorDestinyActivityGraphNodeStateEntry: APIModel {

    public var state: Int?

    public init(state: Int? = nil) {
        self.state = state
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        state = try container.decodeIfPresent("state")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(state, forKey: "state")
    }

    public static func == (lhs: DestinyDefinitionsDirectorDestinyActivityGraphNodeStateEntry, rhs: DestinyDefinitionsDirectorDestinyActivityGraphNodeStateEntry) -> Bool {
        guard lhs.state == rhs.state else { return false }
        return true
    }
}
