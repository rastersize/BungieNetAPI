//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** This defines information that can only come from a talent grid on an item. Items mostly have negligible talent grid data these days, but instanced items still retain grids as a source for some of this common information.
Builds/Subclasses are the only items left that still have talent grids with meaningful Nodes. */
public struct DestinyDefinitionsDestinyItemTalentGridBlockDefinition: APIModel {

    /** A shortcut string identifier for the "build" in question, if this talent grid has an associated build. Doesn't map to anything we can expose at the moment. */
    public var buildName: String?

    /** If the talent grid implies a damage type, this is the enum value for that damage type. */
    public var hudDamageType: Int?

    /** If the talent grid has a special icon that's shown in the game UI (like builds, funny that), this is the identifier for that icon. Sadly, we don't actually get that icon right now. I'll be looking to replace this with a path to the actual icon itself. */
    public var hudIcon: String?

    /** This is meant to be a subtitle for looking at the talent grid. In practice, somewhat frustratingly, this always merely says the localized word for "Details". Great. Maybe it'll have more if talent grids ever get used for more than builds and subclasses again. */
    public var itemDetailString: String?

    /** The hash identifier of the DestinyTalentGridDefinition attached to this item. */
    public var talentGridHash: Int?

    public init(buildName: String? = nil, hudDamageType: Int? = nil, hudIcon: String? = nil, itemDetailString: String? = nil, talentGridHash: Int? = nil) {
        self.buildName = buildName
        self.hudDamageType = hudDamageType
        self.hudIcon = hudIcon
        self.itemDetailString = itemDetailString
        self.talentGridHash = talentGridHash
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        buildName = try container.decodeIfPresent("buildName")
        hudDamageType = try container.decodeIfPresent("hudDamageType")
        hudIcon = try container.decodeIfPresent("hudIcon")
        itemDetailString = try container.decodeIfPresent("itemDetailString")
        talentGridHash = try container.decodeIfPresent("talentGridHash")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(buildName, forKey: "buildName")
        try container.encodeIfPresent(hudDamageType, forKey: "hudDamageType")
        try container.encodeIfPresent(hudIcon, forKey: "hudIcon")
        try container.encodeIfPresent(itemDetailString, forKey: "itemDetailString")
        try container.encodeIfPresent(talentGridHash, forKey: "talentGridHash")
    }

    public static func == (lhs: DestinyDefinitionsDestinyItemTalentGridBlockDefinition, rhs: DestinyDefinitionsDestinyItemTalentGridBlockDefinition) -> Bool {
        guard lhs.buildName == rhs.buildName else { return false }
        guard lhs.hudDamageType == rhs.hudDamageType else { return false }
        guard lhs.hudIcon == rhs.hudIcon else { return false }
        guard lhs.itemDetailString == rhs.itemDetailString else { return false }
        guard lhs.talentGridHash == rhs.talentGridHash else { return false }
        return true
    }
}
