//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** If an item is a Plug, its DestinyInventoryItemDefinition.plug property will be populated with an instance of one of these bad boys.
This gives information about when it can be inserted, what the plug's category is (and thus whether it is compatible with a socket... see DestinySocketTypeDefinition for information about Plug Categories and socket compatibility), whether it is enabled and other Plug info. */
public struct DestinyDefinitionsItemsDestinyItemPlugDefinition: APIModel {

    /** The alternate plug of the plug: only applies when the item is in states that only the server can know about and control, unfortunately. See AlternateUiPlugLabel for the related label info. */
    public var alternatePlugStyle: Int?

    /** If the plug meets certain state requirements, it may have an alternative label applied to it. This is the alternative label that will be applied in such a situation. */
    public var alternateUiPlugLabel: String?

    /** It's not enough for the plug to be inserted. It has to be enabled as well. For it to be enabled, it may require materials. This is the hash identifier for the DestinyMaterialRequirementSetDefinition for those requirements, if there is one. */
    public var enabledMaterialRequirementHash: Int?

    /** The rules around whether the plug, once inserted, is enabled and providing its benefits.
The live data DestinyItemPlugComponent.enableFailIndexes will be an index into this array, so you can pull out the failure strings appropriate for the user. */
    public var enabledRules: [DestinyDefinitionsItemsDestinyPlugRuleDefinition]?

    /** IF not null, this plug provides Energy capacity to the item in which it is socketed. In Armor 2.0 for example, is implemented in a similar way to Masterworks, where visually it's a single area of the UI being clicked on to "Upgrade" to higher energy levels, but it's actually socketing new plugs. */
    public var energyCapacity: [String: Any]?

    /** IF not null, this plug has an energy cost. This contains the details of that cost. */
    public var energyCost: [String: Any]?

    /** If inserting this plug requires materials, this is the hash identifier for looking up the DestinyMaterialRequirementSetDefinition for those requirements. */
    public var insertionMaterialRequirementHash: Int?

    /** The rules around when this plug can be inserted into a socket, aside from the socket's individual restrictions.
The live data DestinyItemPlugComponent.insertFailIndexes will be an index into this array, so you can pull out the failure strings appropriate for the user. */
    public var insertionRules: [DestinyDefinitionsItemsDestinyPlugRuleDefinition]?

    /** If TRUE, this plug is used for UI display purposes only, and doesn't have any interesting effects of its own. */
    public var isDummyPlug: Bool?

    /** If you successfully socket the item, this will determine whether or not you get "refunded" on the plug. */
    public var onActionRecreateSelf: Bool?

    /** Do you ever get the feeling that a system has become so overburdened by edge cases that it probably should have become some other system entirely? So do I!
In totally unrelated news, Plugs can now override properties of their parent items. This is some of the relevant definition data for those overrides.
If this is populated, it will have the override data to be applied when this plug is applied to an item. */
    public var parentItemOverride: [String: Any]?

    /** Indicates the rules about when this plug can be used. See the PlugAvailabilityMode enumeration for more information! */
    public var plugAvailability: Int?

    /** The hash for the plugCategoryIdentifier. You can use this instead if you wish: I put both in the definition for debugging purposes. */
    public var plugCategoryHash: Int?

    /** The string identifier for the plug's category. Use the socket's DestinySocketTypeDefinition.plugWhitelist to determine whether this plug can be inserted into the socket. */
    public var plugCategoryIdentifier: String?

    public var plugStyle: Int?

    /** In the game, if you're inspecting a plug item directly, this will be the item shown with the plug attached. Look up the DestinyInventoryItemDefinition for this hash for the item. */
    public var previewItemOverrideHash: Int?

    /** Plugs can have arbitrary, UI-defined identifiers that the UI designers use to determine the style applied to plugs. Unfortunately, we have neither a definitive list of these labels nor advance warning of when new labels might be applied or how that relates to how they get rendered. If you want to, you can refer to known labels to change your own styles: but know that new ones can be created arbitrarily, and we have no way of associating the labels with any specific UI style guidance... you'll have to piece that together on your end. Or do what we do, and just show plugs more generically, without specialized styles. */
    public var uiPlugLabel: String?

    public init(alternatePlugStyle: Int? = nil, alternateUiPlugLabel: String? = nil, enabledMaterialRequirementHash: Int? = nil, enabledRules: [DestinyDefinitionsItemsDestinyPlugRuleDefinition]? = nil, energyCapacity: [String: Any]? = nil, energyCost: [String: Any]? = nil, insertionMaterialRequirementHash: Int? = nil, insertionRules: [DestinyDefinitionsItemsDestinyPlugRuleDefinition]? = nil, isDummyPlug: Bool? = nil, onActionRecreateSelf: Bool? = nil, parentItemOverride: [String: Any]? = nil, plugAvailability: Int? = nil, plugCategoryHash: Int? = nil, plugCategoryIdentifier: String? = nil, plugStyle: Int? = nil, previewItemOverrideHash: Int? = nil, uiPlugLabel: String? = nil) {
        self.alternatePlugStyle = alternatePlugStyle
        self.alternateUiPlugLabel = alternateUiPlugLabel
        self.enabledMaterialRequirementHash = enabledMaterialRequirementHash
        self.enabledRules = enabledRules
        self.energyCapacity = energyCapacity
        self.energyCost = energyCost
        self.insertionMaterialRequirementHash = insertionMaterialRequirementHash
        self.insertionRules = insertionRules
        self.isDummyPlug = isDummyPlug
        self.onActionRecreateSelf = onActionRecreateSelf
        self.parentItemOverride = parentItemOverride
        self.plugAvailability = plugAvailability
        self.plugCategoryHash = plugCategoryHash
        self.plugCategoryIdentifier = plugCategoryIdentifier
        self.plugStyle = plugStyle
        self.previewItemOverrideHash = previewItemOverrideHash
        self.uiPlugLabel = uiPlugLabel
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        alternatePlugStyle = try container.decodeIfPresent("alternatePlugStyle")
        alternateUiPlugLabel = try container.decodeIfPresent("alternateUiPlugLabel")
        enabledMaterialRequirementHash = try container.decodeIfPresent("enabledMaterialRequirementHash")
        enabledRules = try container.decodeArrayIfPresent("enabledRules")
        energyCapacity = try container.decodeAnyIfPresent("energyCapacity")
        energyCost = try container.decodeAnyIfPresent("energyCost")
        insertionMaterialRequirementHash = try container.decodeIfPresent("insertionMaterialRequirementHash")
        insertionRules = try container.decodeArrayIfPresent("insertionRules")
        isDummyPlug = try container.decodeIfPresent("isDummyPlug")
        onActionRecreateSelf = try container.decodeIfPresent("onActionRecreateSelf")
        parentItemOverride = try container.decodeAnyIfPresent("parentItemOverride")
        plugAvailability = try container.decodeIfPresent("plugAvailability")
        plugCategoryHash = try container.decodeIfPresent("plugCategoryHash")
        plugCategoryIdentifier = try container.decodeIfPresent("plugCategoryIdentifier")
        plugStyle = try container.decodeIfPresent("plugStyle")
        previewItemOverrideHash = try container.decodeIfPresent("previewItemOverrideHash")
        uiPlugLabel = try container.decodeIfPresent("uiPlugLabel")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(alternatePlugStyle, forKey: "alternatePlugStyle")
        try container.encodeIfPresent(alternateUiPlugLabel, forKey: "alternateUiPlugLabel")
        try container.encodeIfPresent(enabledMaterialRequirementHash, forKey: "enabledMaterialRequirementHash")
        try container.encodeIfPresent(enabledRules, forKey: "enabledRules")
        try container.encodeAnyIfPresent(energyCapacity, forKey: "energyCapacity")
        try container.encodeAnyIfPresent(energyCost, forKey: "energyCost")
        try container.encodeIfPresent(insertionMaterialRequirementHash, forKey: "insertionMaterialRequirementHash")
        try container.encodeIfPresent(insertionRules, forKey: "insertionRules")
        try container.encodeIfPresent(isDummyPlug, forKey: "isDummyPlug")
        try container.encodeIfPresent(onActionRecreateSelf, forKey: "onActionRecreateSelf")
        try container.encodeAnyIfPresent(parentItemOverride, forKey: "parentItemOverride")
        try container.encodeIfPresent(plugAvailability, forKey: "plugAvailability")
        try container.encodeIfPresent(plugCategoryHash, forKey: "plugCategoryHash")
        try container.encodeIfPresent(plugCategoryIdentifier, forKey: "plugCategoryIdentifier")
        try container.encodeIfPresent(plugStyle, forKey: "plugStyle")
        try container.encodeIfPresent(previewItemOverrideHash, forKey: "previewItemOverrideHash")
        try container.encodeIfPresent(uiPlugLabel, forKey: "uiPlugLabel")
    }

    public static func == (lhs: DestinyDefinitionsItemsDestinyItemPlugDefinition, rhs: DestinyDefinitionsItemsDestinyItemPlugDefinition) -> Bool {
        guard lhs.alternatePlugStyle == rhs.alternatePlugStyle else { return false }
        guard lhs.alternateUiPlugLabel == rhs.alternateUiPlugLabel else { return false }
        guard lhs.enabledMaterialRequirementHash == rhs.enabledMaterialRequirementHash else { return false }
        guard lhs.enabledRules == rhs.enabledRules else { return false }
        guard NSDictionary(dictionary: lhs.energyCapacity ?? [:]).isEqual(to: rhs.energyCapacity ?? [:]) else { return false }
        guard NSDictionary(dictionary: lhs.energyCost ?? [:]).isEqual(to: rhs.energyCost ?? [:]) else { return false }
        guard lhs.insertionMaterialRequirementHash == rhs.insertionMaterialRequirementHash else { return false }
        guard lhs.insertionRules == rhs.insertionRules else { return false }
        guard lhs.isDummyPlug == rhs.isDummyPlug else { return false }
        guard lhs.onActionRecreateSelf == rhs.onActionRecreateSelf else { return false }
        guard NSDictionary(dictionary: lhs.parentItemOverride ?? [:]).isEqual(to: rhs.parentItemOverride ?? [:]) else { return false }
        guard lhs.plugAvailability == rhs.plugAvailability else { return false }
        guard lhs.plugCategoryHash == rhs.plugCategoryHash else { return false }
        guard lhs.plugCategoryIdentifier == rhs.plugCategoryIdentifier else { return false }
        guard lhs.plugStyle == rhs.plugStyle else { return false }
        guard lhs.previewItemOverrideHash == rhs.previewItemOverrideHash else { return false }
        guard lhs.uiPlugLabel == rhs.uiPlugLabel else { return false }
        return true
    }
}
