//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** If the Milestone or a component has vendors whose inventories could/should be displayed that are relevant to it, this will return the vendor in question. 
It also contains information we need to determine whether that vendor is actually relevant at the moment, given the user's current state. */
public struct DestinyDefinitionsMilestonesDestinyMilestoneVendorDefinition: APIModel {

    /** The hash of the vendor whose wares should be shown as associated with the Milestone. */
    public var vendorHash: Int?

    public init(vendorHash: Int? = nil) {
        self.vendorHash = vendorHash
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        vendorHash = try container.decodeIfPresent("vendorHash")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(vendorHash, forKey: "vendorHash")
    }

    public static func == (lhs: DestinyDefinitionsMilestonesDestinyMilestoneVendorDefinition, rhs: DestinyDefinitionsMilestonesDestinyMilestoneVendorDefinition) -> Bool {
        guard lhs.vendorHash == rhs.vendorHash else { return false }
        return true
    }
}
