//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Talent Grids on items have Nodes. These nodes have positions in the talent grid's UI, and contain "Steps" (DestinyTalentNodeStepDefinition), one of whom will be the "Current" step.
The Current Step determines the visual properties of the node, as well as what the node grants when it is activated.
See DestinyTalentGridDefinition for a more complete overview of how Talent Grids work, and how they are used in Destiny 2 (and how they were used in Destiny 1). */
public struct DestinyDefinitionsDestinyTalentNodeDefinition: APIModel {

    /** If true, this node will automatically unlock when the Talent Grid's level reaches the required level of the current step of this node. */
    public var autoUnlocks: Bool?

    /** At one point, Talent Nodes supported the idea of "Binary Pairs": nodes that overlapped each other visually, and where activating one deactivated the other. They ended up not being used, mostly because Exclusive Sets are *almost* a superset of this concept, but the potential for it to be used still exists in theory.
If this is ever used, this will be the index into the DestinyTalentGridDefinition.nodes property for the node that is the binary pair match to this node. Activating one deactivates the other. */
    public var binaryPairNodeIndex: Int?

    /** The visual "column" where the node should be shown in the UI. If negative, the node is hidden. */
    public var column: Int?

    /** The nodeHash values for nodes that are in an Exclusive Set with this node.
See DestinyTalentGridDefinition.exclusiveSets for more info about exclusive sets.
Again, note that these are nodeHashes and *not* nodeIndexes. */
    public var exclusiveWithNodeHashes: [Int]?

    /** As of Destiny 2, nodes can exist as part of "Exclusive Groups". These differ from exclusive sets in that, within the group, many nodes can be activated. But the act of activating any node in the group will cause "opposing" nodes (nodes in groups that are not allowed to be activated at the same time as this group) to deactivate.
See DestinyTalentExclusiveGroup for more information on the details. This is an identifier for this node's group, if it is part of one. */
    public var groupHash: Int?

    /** Comes from the talent grid node style: if true, then this node should be ignored for determining whether the grid is complete. */
    public var ignoreForCompletion: Bool?

    /** If this is true, the node's step is determined randomly rather than the first step being chosen. */
    public var isRandom: Bool?

    /** If this is true, the node can be "re-rolled" to acquire a different random current step. This is not used, but still exists for a theoretical future of talent grids. */
    public var isRandomRepurchasable: Bool?

    /** At one point, Nodes were going to be able to be activated multiple times, changing the current step and potentially piling on multiple effects from the previously activated steps. This property would indicate if the last step could be activated multiple times. 
This is not currently used, but it isn't out of the question that this could end up being used again in a theoretical future. */
    public var lastStepRepeats: Bool?

    /** A string identifier for a custom visual layout to apply to this talent node. Unfortunately, we do not have any data for rendering these custom layouts. It will be up to you to interpret these strings and change your UI if you want to have custom UI matching these layouts. */
    public var layoutIdentifier: String?

    /** Talent nodes can be associated with a piece of Lore, generally rendered in a tooltip. This is the hash identifier of the lore element to show, if there is one to be show. */
    public var loreHash: Int?

    /** The hash identifier for the node, which unfortunately is also content version dependent but can be (and ideally, should be) used instead of the nodeIndex to uniquely identify the node.
The two exist side-by-side for backcompat reasons due to the Great Talent Node Restructuring of Destiny 1, and I ran out of time to remove one of them and standardize on the other. Sorry! */
    public var nodeHash: Int?

    /** The index into the DestinyTalentGridDefinition's "nodes" property where this node is located. Used to uniquely identify the node within the Talent Grid. Note that this is content version dependent: make sure you have the latest version of content before trying to use these properties. */
    public var nodeIndex: Int?

    /** Comes from the talent grid node style: this identifier should be used to determine how to render the node in the UI. */
    public var nodeStyleIdentifier: String?

    /** Indexes into the DestinyTalentGridDefinition.nodes property for any nodes that must be activated before this one is allowed to be activated.
I would have liked to change this to hashes for Destiny 2, but we have run out of time. */
    public var prerequisiteNodeIndexes: [Int]?

    /** At one point, you were going to be able to repurchase talent nodes that had random steps, to "re-roll" the current step of the node (and thus change the properties of your item). This was to be the activation requirement for performing that re-roll.
The system still exists to do this, as far as I know, so it may yet come back around! */
    public var randomActivationRequirement: [String: Any]?

    /** If the node's step is randomly selected, this is the amount of the Talent Grid's progression experience at which the progression bar for the node should be shown. */
    public var randomStartProgressionBarAtProgression: Int?

    /** The visual "row" where the node should be shown in the UI. If negative, then the node is hidden. */
    public var row: Int?

    /** At this point, "steps" have been obfuscated into conceptual entities, aggregating the underlying notions of "properties" and "true steps".
If you need to know a step as it truly exists - such as when recreating Node logic when processing Vendor data - you'll have to use the "realSteps" property below. */
    public var steps: [DestinyDefinitionsDestinyNodeStepDefinition]?

    public init(autoUnlocks: Bool? = nil, binaryPairNodeIndex: Int? = nil, column: Int? = nil, exclusiveWithNodeHashes: [Int]? = nil, groupHash: Int? = nil, ignoreForCompletion: Bool? = nil, isRandom: Bool? = nil, isRandomRepurchasable: Bool? = nil, lastStepRepeats: Bool? = nil, layoutIdentifier: String? = nil, loreHash: Int? = nil, nodeHash: Int? = nil, nodeIndex: Int? = nil, nodeStyleIdentifier: String? = nil, prerequisiteNodeIndexes: [Int]? = nil, randomActivationRequirement: [String: Any]? = nil, randomStartProgressionBarAtProgression: Int? = nil, row: Int? = nil, steps: [DestinyDefinitionsDestinyNodeStepDefinition]? = nil) {
        self.autoUnlocks = autoUnlocks
        self.binaryPairNodeIndex = binaryPairNodeIndex
        self.column = column
        self.exclusiveWithNodeHashes = exclusiveWithNodeHashes
        self.groupHash = groupHash
        self.ignoreForCompletion = ignoreForCompletion
        self.isRandom = isRandom
        self.isRandomRepurchasable = isRandomRepurchasable
        self.lastStepRepeats = lastStepRepeats
        self.layoutIdentifier = layoutIdentifier
        self.loreHash = loreHash
        self.nodeHash = nodeHash
        self.nodeIndex = nodeIndex
        self.nodeStyleIdentifier = nodeStyleIdentifier
        self.prerequisiteNodeIndexes = prerequisiteNodeIndexes
        self.randomActivationRequirement = randomActivationRequirement
        self.randomStartProgressionBarAtProgression = randomStartProgressionBarAtProgression
        self.row = row
        self.steps = steps
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        autoUnlocks = try container.decodeIfPresent("autoUnlocks")
        binaryPairNodeIndex = try container.decodeIfPresent("binaryPairNodeIndex")
        column = try container.decodeIfPresent("column")
        exclusiveWithNodeHashes = try container.decodeArrayIfPresent("exclusiveWithNodeHashes")
        groupHash = try container.decodeIfPresent("groupHash")
        ignoreForCompletion = try container.decodeIfPresent("ignoreForCompletion")
        isRandom = try container.decodeIfPresent("isRandom")
        isRandomRepurchasable = try container.decodeIfPresent("isRandomRepurchasable")
        lastStepRepeats = try container.decodeIfPresent("lastStepRepeats")
        layoutIdentifier = try container.decodeIfPresent("layoutIdentifier")
        loreHash = try container.decodeIfPresent("loreHash")
        nodeHash = try container.decodeIfPresent("nodeHash")
        nodeIndex = try container.decodeIfPresent("nodeIndex")
        nodeStyleIdentifier = try container.decodeIfPresent("nodeStyleIdentifier")
        prerequisiteNodeIndexes = try container.decodeArrayIfPresent("prerequisiteNodeIndexes")
        randomActivationRequirement = try container.decodeAnyIfPresent("randomActivationRequirement")
        randomStartProgressionBarAtProgression = try container.decodeIfPresent("randomStartProgressionBarAtProgression")
        row = try container.decodeIfPresent("row")
        steps = try container.decodeArrayIfPresent("steps")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(autoUnlocks, forKey: "autoUnlocks")
        try container.encodeIfPresent(binaryPairNodeIndex, forKey: "binaryPairNodeIndex")
        try container.encodeIfPresent(column, forKey: "column")
        try container.encodeIfPresent(exclusiveWithNodeHashes, forKey: "exclusiveWithNodeHashes")
        try container.encodeIfPresent(groupHash, forKey: "groupHash")
        try container.encodeIfPresent(ignoreForCompletion, forKey: "ignoreForCompletion")
        try container.encodeIfPresent(isRandom, forKey: "isRandom")
        try container.encodeIfPresent(isRandomRepurchasable, forKey: "isRandomRepurchasable")
        try container.encodeIfPresent(lastStepRepeats, forKey: "lastStepRepeats")
        try container.encodeIfPresent(layoutIdentifier, forKey: "layoutIdentifier")
        try container.encodeIfPresent(loreHash, forKey: "loreHash")
        try container.encodeIfPresent(nodeHash, forKey: "nodeHash")
        try container.encodeIfPresent(nodeIndex, forKey: "nodeIndex")
        try container.encodeIfPresent(nodeStyleIdentifier, forKey: "nodeStyleIdentifier")
        try container.encodeIfPresent(prerequisiteNodeIndexes, forKey: "prerequisiteNodeIndexes")
        try container.encodeAnyIfPresent(randomActivationRequirement, forKey: "randomActivationRequirement")
        try container.encodeIfPresent(randomStartProgressionBarAtProgression, forKey: "randomStartProgressionBarAtProgression")
        try container.encodeIfPresent(row, forKey: "row")
        try container.encodeIfPresent(steps, forKey: "steps")
    }

    public static func == (lhs: DestinyDefinitionsDestinyTalentNodeDefinition, rhs: DestinyDefinitionsDestinyTalentNodeDefinition) -> Bool {
        guard lhs.autoUnlocks == rhs.autoUnlocks else { return false }
        guard lhs.binaryPairNodeIndex == rhs.binaryPairNodeIndex else { return false }
        guard lhs.column == rhs.column else { return false }
        guard lhs.exclusiveWithNodeHashes == rhs.exclusiveWithNodeHashes else { return false }
        guard lhs.groupHash == rhs.groupHash else { return false }
        guard lhs.ignoreForCompletion == rhs.ignoreForCompletion else { return false }
        guard lhs.isRandom == rhs.isRandom else { return false }
        guard lhs.isRandomRepurchasable == rhs.isRandomRepurchasable else { return false }
        guard lhs.lastStepRepeats == rhs.lastStepRepeats else { return false }
        guard lhs.layoutIdentifier == rhs.layoutIdentifier else { return false }
        guard lhs.loreHash == rhs.loreHash else { return false }
        guard lhs.nodeHash == rhs.nodeHash else { return false }
        guard lhs.nodeIndex == rhs.nodeIndex else { return false }
        guard lhs.nodeStyleIdentifier == rhs.nodeStyleIdentifier else { return false }
        guard lhs.prerequisiteNodeIndexes == rhs.prerequisiteNodeIndexes else { return false }
        guard NSDictionary(dictionary: lhs.randomActivationRequirement ?? [:]).isEqual(to: rhs.randomActivationRequirement ?? [:]) else { return false }
        guard lhs.randomStartProgressionBarAtProgression == rhs.randomStartProgressionBarAtProgression else { return false }
        guard lhs.row == rhs.row else { return false }
        guard lhs.steps == rhs.steps else { return false }
        return true
    }
}
