//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Request this component if you want the details about an item being sold in relation to the character making the request: whether the character can buy it, whether they can afford it, and other data related to purchasing the item.
Note that if you want instance, stats, etc... data for the item, you'll have to request additional components such as ItemInstances, ItemPerks etc... and acquire them from the DestinyVendorResponse's "items" property. */
public struct DestinyEntitiesVendorsDestinyVendorSaleItemComponent: APIModel {

    /** A flags enumeration value representing the current state of any "state modifiers" on the item being sold. These are meant to correspond with some sort of visual indicator as to the augmentation: for instance, if an item is on sale or if you already own the item in question.
Determining how you want to represent these in your own app (or if you even want to) is an exercise left for the reader. */
    public var augments: Int?

    /** A summary of the current costs of the item. */
    public var costs: [DestinyDestinyItemQuantity]?

    /** Indexes in to the "failureStrings" lookup table in DestinyVendorDefinition for the given Vendor. Gives some more reliable failure information for why you can't purchase an item.
It is preferred to use these over requiredUnlocks and unlockStatuses: the latter are provided mostly in case someone can do something interesting with it that I didn't anticipate. */
    public var failureIndexes: [Int]?

    /** The hash of the item being sold, as a quick shortcut for looking up the DestinyInventoryItemDefinition of the sale item. */
    public var itemHash: Int?

    /** If this item has its own custom date where it may be removed from the Vendor's rotation, this is that date.
Note that there's not actually any guarantee that it will go away: it could be chosen again and end up still being in the Vendor's sale items! But this is the next date where that test will occur, and is also the date that the game shows for availability on things like Bounties being sold. So it's the best we can give. */
    public var overrideNextRefreshDate: DateTime?

    /** If populated, this is the hash of the item whose icon (and other secondary styles, but *not* the human readable strings) should override whatever icons/styles are on the item being sold.
If you don't do this, certain items whose styles are being overridden by socketed items - such as the "Recycle Shader" item - would show whatever their default icon/style is, and it wouldn't be pretty or look accurate. */
    public var overrideStyleItemHash: Int?

    /** How much of the item you'll be getting. */
    public var quantity: Int?

    /** If you can't buy the item due to a complex character state, these will be hashes for DestinyUnlockDefinitions that you can check to see messages regarding the failure (if the unlocks have human readable information: it is not guaranteed that Unlocks will have human readable strings, and your application will have to handle that)
Prefer using failureIndexes instead. These are provided for informational purposes, but have largely been supplanted by failureIndexes. */
    public var requiredUnlocks: [Int]?

    /** A flag indicating whether the requesting character can buy the item, and if not the reasons why the character can't buy it. */
    public var saleStatus: Int?

    /** If any complex unlock states are checked in determining purchasability, these will be returned here along with the status of the unlock check.
Prefer using failureIndexes instead. These are provided for informational purposes, but have largely been supplanted by failureIndexes. */
    public var unlockStatuses: [DestinyDestinyUnlockStatus]?

    /** The index into the DestinyVendorDefinition.itemList property. Note that this means Vendor data *is* Content Version dependent: make sure you have the latest content before you use Vendor data, or these indexes may mismatch. 
Most systems avoid this problem, but Vendors is one area where we are unable to reasonably avoid content dependency at the moment. */
    public var vendorItemIndex: Int?

    public init(augments: Int? = nil, costs: [DestinyDestinyItemQuantity]? = nil, failureIndexes: [Int]? = nil, itemHash: Int? = nil, overrideNextRefreshDate: DateTime? = nil, overrideStyleItemHash: Int? = nil, quantity: Int? = nil, requiredUnlocks: [Int]? = nil, saleStatus: Int? = nil, unlockStatuses: [DestinyDestinyUnlockStatus]? = nil, vendorItemIndex: Int? = nil) {
        self.augments = augments
        self.costs = costs
        self.failureIndexes = failureIndexes
        self.itemHash = itemHash
        self.overrideNextRefreshDate = overrideNextRefreshDate
        self.overrideStyleItemHash = overrideStyleItemHash
        self.quantity = quantity
        self.requiredUnlocks = requiredUnlocks
        self.saleStatus = saleStatus
        self.unlockStatuses = unlockStatuses
        self.vendorItemIndex = vendorItemIndex
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        augments = try container.decodeIfPresent("augments")
        costs = try container.decodeArrayIfPresent("costs")
        failureIndexes = try container.decodeArrayIfPresent("failureIndexes")
        itemHash = try container.decodeIfPresent("itemHash")
        overrideNextRefreshDate = try container.decodeIfPresent("overrideNextRefreshDate")
        overrideStyleItemHash = try container.decodeIfPresent("overrideStyleItemHash")
        quantity = try container.decodeIfPresent("quantity")
        requiredUnlocks = try container.decodeArrayIfPresent("requiredUnlocks")
        saleStatus = try container.decodeIfPresent("saleStatus")
        unlockStatuses = try container.decodeArrayIfPresent("unlockStatuses")
        vendorItemIndex = try container.decodeIfPresent("vendorItemIndex")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(augments, forKey: "augments")
        try container.encodeIfPresent(costs, forKey: "costs")
        try container.encodeIfPresent(failureIndexes, forKey: "failureIndexes")
        try container.encodeIfPresent(itemHash, forKey: "itemHash")
        try container.encodeIfPresent(overrideNextRefreshDate, forKey: "overrideNextRefreshDate")
        try container.encodeIfPresent(overrideStyleItemHash, forKey: "overrideStyleItemHash")
        try container.encodeIfPresent(quantity, forKey: "quantity")
        try container.encodeIfPresent(requiredUnlocks, forKey: "requiredUnlocks")
        try container.encodeIfPresent(saleStatus, forKey: "saleStatus")
        try container.encodeIfPresent(unlockStatuses, forKey: "unlockStatuses")
        try container.encodeIfPresent(vendorItemIndex, forKey: "vendorItemIndex")
    }

    public static func == (lhs: DestinyEntitiesVendorsDestinyVendorSaleItemComponent, rhs: DestinyEntitiesVendorsDestinyVendorSaleItemComponent) -> Bool {
        guard lhs.augments == rhs.augments else { return false }
        guard lhs.costs == rhs.costs else { return false }
        guard lhs.failureIndexes == rhs.failureIndexes else { return false }
        guard lhs.itemHash == rhs.itemHash else { return false }
        guard lhs.overrideNextRefreshDate == rhs.overrideNextRefreshDate else { return false }
        guard lhs.overrideStyleItemHash == rhs.overrideStyleItemHash else { return false }
        guard lhs.quantity == rhs.quantity else { return false }
        guard lhs.requiredUnlocks == rhs.requiredUnlocks else { return false }
        guard lhs.saleStatus == rhs.saleStatus else { return false }
        guard lhs.unlockStatuses == rhs.unlockStatuses else { return false }
        guard lhs.vendorItemIndex == rhs.vendorItemIndex else { return false }
        return true
    }
}
