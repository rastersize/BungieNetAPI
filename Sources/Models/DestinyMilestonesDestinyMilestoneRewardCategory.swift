//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** Represents a category of "summary" rewards that can be earned for the Milestone regardless of specific quest rewards that can be earned. */
public struct DestinyMilestonesDestinyMilestoneRewardCategory: APIModel {

    /** The individual reward entries for this category, and their status. */
    public var entries: [DestinyMilestonesDestinyMilestoneRewardEntry]?

    /** Look up the relevant DestinyMilestoneDefinition, and then use rewardCategoryHash to look up the category info in DestinyMilestoneDefinition.rewards. */
    public var rewardCategoryHash: Int?

    public init(entries: [DestinyMilestonesDestinyMilestoneRewardEntry]? = nil, rewardCategoryHash: Int? = nil) {
        self.entries = entries
        self.rewardCategoryHash = rewardCategoryHash
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        entries = try container.decodeArrayIfPresent("entries")
        rewardCategoryHash = try container.decodeIfPresent("rewardCategoryHash")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(entries, forKey: "entries")
        try container.encodeIfPresent(rewardCategoryHash, forKey: "rewardCategoryHash")
    }

    public static func == (lhs: DestinyMilestonesDestinyMilestoneRewardCategory, rhs: DestinyMilestonesDestinyMilestoneRewardCategory) -> Bool {
        guard lhs.entries == rhs.entries else { return false }
        guard lhs.rewardCategoryHash == rhs.rewardCategoryHash else { return false }
        return true
    }
}
