//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** This component returns anything that could be considered "Progression" on a user: data where the user is gaining levels, reputation, completions, rewards, etc... */
public struct DestinyEntitiesCharactersDestinyCharacterProgressionComponent: APIModel {

    /** The set of checklists that can be examined for this specific character, keyed by the hash identifier of the Checklist (DestinyChecklistDefinition)
For each checklist returned, its value is itself a Dictionary keyed by the checklist's hash identifier with the value being a boolean indicating if it's been discovered yet. */
    public var checklists: [String: [String: Bool]]?

    /** A dictionary of all known Factions, keyed by the Faction's hash. It contains data about this character's status with the faction. */
    public var factions: [String: DestinyProgressionDestinyFactionProgression]?

    /** Milestones are related to the simple progressions shown in the game, but return additional and hopefully helpful information for users about the specifics of the Milestone's status. */
    public var milestones: [String: DestinyMilestonesDestinyMilestone]?

    /** A Dictionary of all known progressions for the Character, keyed by the Progression's hash.
Not all progressions have user-facing data, but those who do will have that data contained in the DestinyProgressionDefinition. */
    public var progressions: [String: DestinyDestinyProgression]?

    /** If the user has any active quests, the quests' statuses will be returned here.
 Note that quests have been largely supplanted by Milestones, but that doesn't mean that they won't make a comeback independent of milestones at some point.
 (Fun fact: quests came back as I feared they would, but we never looped back to populate this... I'm going to put that in the backlog.) */
    public var quests: [DestinyQuestsDestinyQuestStatus]?

    /** Data related to your progress on the current season's artifact that can vary per character. */
    public var seasonalArtifact: [String: Any]?

    /** Sometimes, you have items in your inventory that don't have instances, but still have Objective information. This provides you that objective information for uninstanced items. 
This dictionary is keyed by the item's hash: which you can use to look up the name and description for the overall task(s) implied by the objective. The value is the list of objectives for this item, and their statuses. */
    public var uninstancedItemObjectives: [String: [DestinyQuestsDestinyObjectiveProgress]]?

    public init(checklists: [String: [String: Bool]]? = nil, factions: [String: DestinyProgressionDestinyFactionProgression]? = nil, milestones: [String: DestinyMilestonesDestinyMilestone]? = nil, progressions: [String: DestinyDestinyProgression]? = nil, quests: [DestinyQuestsDestinyQuestStatus]? = nil, seasonalArtifact: [String: Any]? = nil, uninstancedItemObjectives: [String: [DestinyQuestsDestinyObjectiveProgress]]? = nil) {
        self.checklists = checklists
        self.factions = factions
        self.milestones = milestones
        self.progressions = progressions
        self.quests = quests
        self.seasonalArtifact = seasonalArtifact
        self.uninstancedItemObjectives = uninstancedItemObjectives
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        checklists = try container.decodeIfPresent("checklists")
        factions = try container.decodeIfPresent("factions")
        milestones = try container.decodeIfPresent("milestones")
        progressions = try container.decodeIfPresent("progressions")
        quests = try container.decodeArrayIfPresent("quests")
        seasonalArtifact = try container.decodeAnyIfPresent("seasonalArtifact")
        uninstancedItemObjectives = try container.decodeIfPresent("uninstancedItemObjectives")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(checklists, forKey: "checklists")
        try container.encodeIfPresent(factions, forKey: "factions")
        try container.encodeIfPresent(milestones, forKey: "milestones")
        try container.encodeIfPresent(progressions, forKey: "progressions")
        try container.encodeIfPresent(quests, forKey: "quests")
        try container.encodeAnyIfPresent(seasonalArtifact, forKey: "seasonalArtifact")
        try container.encodeIfPresent(uninstancedItemObjectives, forKey: "uninstancedItemObjectives")
    }

    public static func == (lhs: DestinyEntitiesCharactersDestinyCharacterProgressionComponent, rhs: DestinyEntitiesCharactersDestinyCharacterProgressionComponent) -> Bool {
        guard lhs.checklists == rhs.checklists else { return false }
        guard lhs.factions == rhs.factions else { return false }
        guard lhs.milestones == rhs.milestones else { return false }
        guard lhs.progressions == rhs.progressions else { return false }
        guard lhs.quests == rhs.quests else { return false }
        guard NSDictionary(dictionary: lhs.seasonalArtifact ?? [:]).isEqual(to: rhs.seasonalArtifact ?? [:]) else { return false }
        guard lhs.uninstancedItemObjectives == rhs.uninstancedItemObjectives else { return false }
        return true
    }
}
