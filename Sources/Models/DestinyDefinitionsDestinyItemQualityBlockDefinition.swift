//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** An item's "Quality" determines its calculated stats. The Level at which the item spawns is combined with its "qualityLevel" along with some additional calculations to determine the value of those stats.
In Destiny 2, most items don't have default item levels and quality, making this property less useful: these apparently are almost always determined by the complex mechanisms of the Reward system rather than statically. They are still provided here in case they are still useful for people. This also contains some information about Infusion. */
public struct DestinyDefinitionsDestinyItemQualityBlockDefinition: APIModel {

    /** The hash identifier for the infusion. It does not map to a Definition entity.
DEPRECATED: Items can now have multiple infusion categories. Please use infusionCategoryHashes instead. */
    public var infusionCategoryHash: Int?

    /** If any one of these hashes matches any value in another item's infusionCategoryHashes, the two can infuse with each other. */
    public var infusionCategoryHashes: [Int]?

    /** The string identifier for this item's "infusability", if any. 
Items that match the same infusionCategoryName are allowed to infuse with each other.
DEPRECATED: Items can now have multiple infusion categories. Please use infusionCategoryHashes instead. */
    public var infusionCategoryName: String?

    /** The "base" defined level of an item. This is a list because, in theory, each Expansion could define its own base level for an item.
In practice, not only was that never done in Destiny 1, but now this isn't even populated at all. When it's not populated, the level at which it spawns has to be inferred by Reward information, of which BNet receives an imperfect view and will only be reliable on instanced data as a result. */
    public var itemLevels: [Int]?

    /** An item can refer to pre-set level requirements. They are defined in DestinyProgressionLevelRequirementDefinition, and you can use this hash to find the appropriate definition. */
    public var progressionLevelRequirementHash: Int?

    /** qualityLevel is used in combination with the item's level to calculate stats like Attack and Defense. It plays a role in that calculation, but not nearly as large as itemLevel does. */
    public var qualityLevel: Int?

    public init(infusionCategoryHash: Int? = nil, infusionCategoryHashes: [Int]? = nil, infusionCategoryName: String? = nil, itemLevels: [Int]? = nil, progressionLevelRequirementHash: Int? = nil, qualityLevel: Int? = nil) {
        self.infusionCategoryHash = infusionCategoryHash
        self.infusionCategoryHashes = infusionCategoryHashes
        self.infusionCategoryName = infusionCategoryName
        self.itemLevels = itemLevels
        self.progressionLevelRequirementHash = progressionLevelRequirementHash
        self.qualityLevel = qualityLevel
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        infusionCategoryHash = try container.decodeIfPresent("infusionCategoryHash")
        infusionCategoryHashes = try container.decodeArrayIfPresent("infusionCategoryHashes")
        infusionCategoryName = try container.decodeIfPresent("infusionCategoryName")
        itemLevels = try container.decodeArrayIfPresent("itemLevels")
        progressionLevelRequirementHash = try container.decodeIfPresent("progressionLevelRequirementHash")
        qualityLevel = try container.decodeIfPresent("qualityLevel")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeIfPresent(infusionCategoryHash, forKey: "infusionCategoryHash")
        try container.encodeIfPresent(infusionCategoryHashes, forKey: "infusionCategoryHashes")
        try container.encodeIfPresent(infusionCategoryName, forKey: "infusionCategoryName")
        try container.encodeIfPresent(itemLevels, forKey: "itemLevels")
        try container.encodeIfPresent(progressionLevelRequirementHash, forKey: "progressionLevelRequirementHash")
        try container.encodeIfPresent(qualityLevel, forKey: "qualityLevel")
    }

    public static func == (lhs: DestinyDefinitionsDestinyItemQualityBlockDefinition, rhs: DestinyDefinitionsDestinyItemQualityBlockDefinition) -> Bool {
        guard lhs.infusionCategoryHash == rhs.infusionCategoryHash else { return false }
        guard lhs.infusionCategoryHashes == rhs.infusionCategoryHashes else { return false }
        guard lhs.infusionCategoryName == rhs.infusionCategoryName else { return false }
        guard lhs.itemLevels == rhs.itemLevels else { return false }
        guard lhs.progressionLevelRequirementHash == rhs.progressionLevelRequirementHash else { return false }
        guard lhs.qualityLevel == rhs.qualityLevel else { return false }
        return true
    }
}
