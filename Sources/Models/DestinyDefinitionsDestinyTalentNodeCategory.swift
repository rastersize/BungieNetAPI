//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

/** An artificial construct provided by Bungie.Net, where we attempt to group talent nodes by functionality.
This is a single set of references to Talent Nodes that share a common trait or purpose. */
public struct DestinyDefinitionsDestinyTalentNodeCategory: APIModel {

    /** Will contain at least the "name", which will be the title of the category. We will likely not have description and an icon yet, but I'm going to keep my options open. */
    public var displayProperties: [String: Any]?

    /** Mostly just for debug purposes, but if you find it useful you can have it. This is BNet's manually created identifier for this category. */
    public var identifier: String?

    /** If true, we found the localized content in a related DestinyLoreDefinition instead of local BNet localization files. This is mostly for ease of my own future investigations. */
    public var isLoreDriven: Bool?

    /** The set of all hash identifiers for Talent Nodes (DestinyTalentNodeDefinition) in this Talent Grid that are part of this Category. */
    public var nodeHashes: [Int]?

    public init(displayProperties: [String: Any]? = nil, identifier: String? = nil, isLoreDriven: Bool? = nil, nodeHashes: [Int]? = nil) {
        self.displayProperties = displayProperties
        self.identifier = identifier
        self.isLoreDriven = isLoreDriven
        self.nodeHashes = nodeHashes
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringCodingKey.self)

        displayProperties = try container.decodeAnyIfPresent("displayProperties")
        identifier = try container.decodeIfPresent("identifier")
        isLoreDriven = try container.decodeIfPresent("isLoreDriven")
        nodeHashes = try container.decodeArrayIfPresent("nodeHashes")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringCodingKey.self)

        try container.encodeAnyIfPresent(displayProperties, forKey: "displayProperties")
        try container.encodeIfPresent(identifier, forKey: "identifier")
        try container.encodeIfPresent(isLoreDriven, forKey: "isLoreDriven")
        try container.encodeIfPresent(nodeHashes, forKey: "nodeHashes")
    }

    public static func == (lhs: DestinyDefinitionsDestinyTalentNodeCategory, rhs: DestinyDefinitionsDestinyTalentNodeCategory) -> Bool {
        guard NSDictionary(dictionary: lhs.displayProperties ?? [:]).isEqual(to: rhs.displayProperties ?? [:]) else { return false }
        guard lhs.identifier == rhs.identifier else { return false }
        guard lhs.isLoreDriven == rhs.isLoreDriven else { return false }
        guard lhs.nodeHashes == rhs.nodeHashes else { return false }
        return true
    }
}
